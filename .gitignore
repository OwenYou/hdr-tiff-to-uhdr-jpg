# Python
.venv/
__pycache__/
*.py[cod]

# Native dependencies (cloned separately, built locally)
libultrahdr/
libjpeg-turbo/

# Built DLLs (exclude the two runtime DLLs needed to run the pipeline)
*.lib
*.exp
*.exe
*.dll
!uhdr.dll
!jpeg62.dll

# Test images and pipeline outputs (large binaries)
test_files/

# Editor / OS
.vscode/
*.DS_Store
Thumbs.db
