# Python
.venv/
__pycache__/
*.py[cod]

# Native dependencies (cloned separately, built locally)
libultrahdr/
libjpeg-turbo/

# Built artifacts — keep only the pre-built runtime libraries needed to run the pipeline
*.lib
*.exp
*.exe
*.dll
!uhdr.dll
!jpeg62.dll
*.dylib
!libuhdr.dylib
!libjpeg.62.dylib

# Test images and pipeline outputs (large binaries)
test_files/

# Editor / OS
.vscode/
*.DS_Store
Thumbs.db
